const config = {
    pageWidth: 3300,
    pageHeight: 2550,
    textSize: 75,
    // Note: there is a small amount of top margin with text so we need to offset it
    textOffset: 22,
};

module.exports = {
    config,
}